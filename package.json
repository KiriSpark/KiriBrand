{
  "name": "kiribrand",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "gulp build | (npm run build-dotnet && npm run build-pages)",
    "build-dotnet": "cd src && dotnet build",
    "build-pages": "cd src && dotnet run",
    "clean": "gulp clean",
    "dev": "gulp watch"
  },
  "author": "Rickas Razafison",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.18.12",
    "del": "^2.2.2",
    "gulp": "~3.9.1",
    "gulp-changed": "^3.1.0",
    "gulp-util": "^3.0.8"
  }
}